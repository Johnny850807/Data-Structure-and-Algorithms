package dsa.algorithms;

/**
 * Some famous algorithms but are not classified into any of the strategy used, that is, they are not any of the
 * application of greedy, dynamic programming, ...etc.
 */
public interface OtherAlgorithms {

    public int maximumArraySum(int[] nums);

    public int celebrity(int n);
}
